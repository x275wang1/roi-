import React from 'react';
import ReactDOM from 'react-dom/client';
import DecarbonizationROIModel from './DecarbonizationROIModel';
import './index.css'; // 可选，用于全局样式


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DecarbonizationROIModel />
  </React.StrictMode>
);